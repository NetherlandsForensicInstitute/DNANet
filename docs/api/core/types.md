# Type Aliases

The `dnanet.core.types` module defines shared type aliases used across the
project.

## PathLike

A file-system path, either as a string or a `pathlib.Path`.

```python
from dnanet.core.types import PathLike
```
